<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:id="@android:id/background">
        <shape
            android:shape="ring"
            android:useLevel="false"
            android:thickness="25dp" android:innerRadius="75dp"> <solid android:color="@color/water_level_background_grey" />
        </shape>
    </item>
    <item android:id="@android:id/progress">
        <rotate
            android:fromDegrees="270"
            android:toDegrees="270"
            android:pivotX="50%"
            android:pivotY="50%">
            <shape
                android:shape="ring"
                android:useLevel="true"
                android:thickness="25dp" android:innerRadius="75dp">
                <solid android:color="@color/water_level_progress_blue" />
            </shape>
        </rotate>
    </item>
</layer-list>